#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    rows=len(matrix)
    columns=len(matrix[0])
    i=0
    j=0
    while i<row:
            while j<column:
                print(matrix[i][j],end=' ')
                j += 1
            print("\n")
            i += 1
